<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Coffee Shop</title>
  <style>
    body { font-family: sans-serif; background-color: #fdf6e3; }
    .menu { padding: 20px; }
    .item { border-bottom: 1px solid #ccc; margin-bottom: 10px; padding: 10px; }
    .name { font-weight: bold; font-size: 1.2em; }
  </style>
</head>
<body>
  <h1 style="text-align:center;">Coffee Menu</h1>
  <div class="menu">
    <% coffees.forEach(coffee => { %>
      <div class="item">
        <div class="name"><%= coffee.name %> (<%= coffee.size %>)</div>
        <div>Price: <%= coffee.price %> THB</div>
      </div>
    <% }) %>
  </div>
</body>
</html>